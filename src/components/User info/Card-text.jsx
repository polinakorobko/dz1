import React from "react";

const UserText = ({ info }) => {
    return (
        <>
            <div>
                <span>{info}</span>
            </div>
        </>
    );
};

export default UserText;